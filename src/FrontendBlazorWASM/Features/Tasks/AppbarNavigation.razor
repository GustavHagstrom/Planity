@inject IStringLocalizer<AppbarNavigation> Localizer

<FeatureNavigationMenu FeatureRoute="@Routes.TasksOverview" MenuTitle="@Localizer["Uppgifter"]" Match="NavLinkMatch.Prefix"/>


@code {
    List<MenuLink> menuLinks = new();

    protected override void OnInitialized()
    {
        menuLinks = new()
        {
            new MenuLink(Localizer["Översikt"], Routes.TasksOverview, null, NavLinkMatch.All),

        };
    }
}
